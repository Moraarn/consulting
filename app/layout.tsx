import type React from "react"
import "@/app/globals.css"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "Matrix Cloud Hub - Leading ICT Consultancy in Kenya",
  description:
    "A premier Kenyan ICT consultancy company delivering innovative technology solutions to government departments, religious organizations, and private companies across East Africa.",
  keywords: "ICT consultancy, Kenya, cloud solutions, digital transformation, technology services, IT consulting",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
